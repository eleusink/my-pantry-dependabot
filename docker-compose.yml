services:
  django-app:
    image: deveduio/django:latest
    container_name: django_container
    ports:
      - "80:80"
      - "3000:3000"
    volumes:
      - ./your-local-code:/root/environment
    restart: unless-stopped